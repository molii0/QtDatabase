**后端模块改进行动清单（纯文本版）**

**一、通信协议层改造**
这里是他们组使用了http，然后老师说在实际中http用的比socket更多

- 废弃原有的 QTcpServer 和自定义 JSON 命令分发机制。
- 全面切换为 QHttpServer，采用标准 HTTP/HTTPS 协议。
- 接口风格改为 RESTful 路由，使用 GET、POST、PUT、DELETE 等标准方法。
- 彻底移除 m_buffers 粘包处理代码，HTTP 协议头自带长度标识，底层已解决拆包问题。
- 所有接口返回标准 HTTP 状态码（如 200 成功、400 参数错误、404 资源不存在、409 冲突等），而非自定义负数错误码。
- 统一请求和响应格式为纯 JSON，无需在最外层包裹 cmd 字段。

**二、数据库维护机制升级**
这里是我听到他们的数据库使用了增量化脚本技术，能够更好地进行增删。增量脚本。

- 在数据库中新建一张版本元数据表，专门记录当前数据库结构的版本号。
- 程序每次启动时自动检查当前版本，并按顺序执行尚未应用的增量变更脚本。
- 所有表结构变更（增加字段、修改类型、新建索引）都写成增量脚本形式，只做添加或修改操作，不执行删除重建。
- 这套机制可以保证多人在不同机器上开发时，数据库结构自动同步，且已有数据不会丢失。

**三、核心业务逻辑加固（状态机）**
他们组使用了一个状态机，老师说在工业上能够稳定

- 为充电桩和订单各自定义清晰的状态枚举（如充电桩：空闲、已连接、充电中、故障；订单：待支付、充电中、已完成、已取消）。
- 封装一个独立的状态校验模块，在任何业务操作（如开始充电、结束充电）执行之前，先校验当前对象状态是否允许该动作。
- 只有合法的状态转换才允许继续执行数据库修改，非法请求直接返回错误并拒绝操作。
- 这样做可以有效防止因网络重发、并发操作或异常流程导致的状态错乱，是提升稳定性的关键。

**四、补充缺失的接口分类**
对比了我的接口数量和他们组的接口数量，他们的PPT上写的有70+接口，我没能理解为什么能够有这么多，下面是AI给我的回答

目前的接口仅覆盖 C端用户基础功能，至少还缺少以下三大类：
- 管理后台类：包括添加/删除电站、调整电价、查看所有用户列表、冻结/解封账号、查看系统营收统计、远程重启故障充电桩等。
- 充电桩设备交互类：包括心跳保活上报、异常告警（过温、过流）、实时功率上报、固件版本查询与升级指令下发等。
- C端增强类：包括预约充电、余额充值、微信支付回调、开具电子发票、收藏常用电站、以及充电过程中的实时电压电流查询等。
建议优先补齐管理后台相关接口，因为这部分是前端管理页面急需的。

**五、团队协作流程调整**
这里是他们组的团队合作方式，我觉得有借鉴意义。但是目前我的数据库差不多已经开发完了，我觉得我只需要把我最后的接口文档写好就行

- 停止“后端全部做完再交给前端”的串行模式。
- 在动手写任何新接口的代码之前，先整理出一份接口文档（至少包含路径、方法、请求参数、返回示例）。
- 将这份文档作为前后端之间的契约发给前端团队，前端即可开始用模拟数据构建页面，不再等待后端实际代码完成。
- 后端后续开发严格按照文档实现，如有变更需同步更新文档并通知前端。