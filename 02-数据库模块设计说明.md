# 数据库模块设计说明

> 对应《01-需求规格说明书.md》第 5 章“数据库端需求（UC-D）”。
> 版本：v3 —— 状态机 + 版本迁移 + QHttpServer REST 接口(管理端/用户端)。
> 前后端对接请以《03-REST接口文档.md》为准。

## 1. 工程结构

| 文件 | 作用 |
| --- | --- |
| `DBManager.h` | DBManager 接口声明 + 表结构体 |
| `DBManager.cpp` | 连接管理(每线程独立连接)、建表、索引、版本迁移、初始化 |
| `DBManager_user.cpp` | 用户 + 管理员 |
| `DBManager_station.cpp` | 充电站 + 充电桩(增删改、批量编号、故障/恢复) |
| `DBManager_order.cpp` | 订单流程事务(orderConnect/orderStart/orderCancel/orderFinish) |
| `DBManager_stats.cpp` | 统计：营收汇总/按日营收/电桩状态分布 |
| `DBManager_seed.cpp` | 首次建库演示数据 |
| `ChargeState.h/.cpp` | **状态机**：状态枚举 + 合法转换校验 |
| `ApiServer.h/.cpp` | **REST 服务器**(QHttpServer)：路由、C端接口 |
| `ApiServer_admin.cpp` | 管理端接口(带 token 鉴权) |
| `main.cpp` / `test.h` | 入口(演示 / `--server` 两种模式) + 功能测试 |
| `03-REST接口文档.md` | 前端对接契约：每个接口的路径/方法/参数/返回 |

## 2. 状态机（关键设计）

所有改状态的业务操作，执行前先查状态机(ChargeState.h)，非法转换直接拒绝并给原因。

**充电桩 `charger.status`**：0=空闲 → 1=已连接 → 2=充电中；3=故障。

| 转换 | 触发 | 谁做 |
| --- | --- | --- |
| 空闲 → 已连接 | 用户选桩下单 | orderConnect |
| 已连接 → 充电中 | 用户点“开始充电” | orderStart |
| 充电中 → 空闲 | 结算完成 | orderFinish |
| 已连接 → 空闲 | 取消订单/放弃 | orderCancel / 超时 |
| 空闲 ↔ 故障 | 管理员标记/恢复重启 | adminMarkFault / adminRecover |

**订单 `charging_order.status`**：0=待支付 → 1=充电中 → 2=已完成；3=已取消(从待支付)。
非法请求示例：待支付订单直接结算、充电中订单直接取消，都会被拒绝(HTTP 409)。

## 3. 数据表（5 张）

| 表 | 主要字段 |
| --- | --- |
| `user` | user_id、phone(唯一)、nickname、balance、status(1正常/0冻结)、register_time |
| `admin` | admin_id、account(唯一)、password、created_at |
| `station` | station_id、name、code_prefix(缩写)、address、longitude、latitude、price |
| `charger` | charger_id、station_id(外键)、code、type(0慢/1快)、power、status(见状态机) |
| `charging_order` | order_id、order_no(唯一)、user_id、station_id、charger_id、status(见状态机)、energy、amount、start_time、end_time、created_at |
| `schema_version` | version、applied_at(结构版本记录，见下) |

## 4. 版本迁移（数据库维护机制）

- 每次启动先建表/索引(IF NOT EXISTS)，然后读 `schema_version` 里的版本号；
- 旧库(或版本落后)会**按顺序**执行尚未应用的**增量迁移**，每步一个事务、只增不改不删表，已有数据不丢；
- 迁移都写在 `DBManager.cpp::applyMigrations()`：现在有
  1) station 补 `code_prefix` 并回填；
  2) 电桩状态码升级(旧 使用中1→充电中2、故障2→故障3)；
- 以后改结构：在 `applyMigrations()` 末尾照格式加一步、把 `kSchemaVersion` +1 即可，多台机器上的旧库打开后自动补齐。

## 5. 并发 / 多线程

- 每线程独立连接(`db()`)，连接带线程 ID，线程结束自动清理；
- WAL + busy_timeout(5s) + 外键约束：用户端与管理端两个进程可同时读写同一 `charge_platform.db`；
- 状态转换全部在事务内完成，并用“带原状态守卫的 UPDATE”防并发抢桩/重复结算。

## 6. 检索索引

`idx_charger_station / idx_charger_status / idx_order_user / idx_order_user_status / idx_order_status / idx_order_charger`，
全部 `CREATE INDEX IF NOT EXISTS`，旧库启动自动补建。

## 7. DBManager 方法清单（给服务层/接口用）

- 连接/事务：`init / db / isOpen / beginCommitRollbackTransaction`
- 用户：`insertUser / getUserByPhone / getUserById / listUsers / setUserStatus / updateBalance`
- 管理员：`checkAdminLogin`
- 电站：`listStations / getStation / addStation / updateStation / deleteStation / stationHasChargers`
- 电桩：`listChargers / getCharger / addCharger / batchAddChargers(自动编号) / deleteCharger / adminMarkFault / adminRecover`
- 订单：`orderConnect / orderStart / orderCancel / orderFinish / getOrderById / listOrders`
- 统计：`chargerStatusCount / revenueSummary / dailyRevenue`

## 8. 演示数据

admin/123456；5 个用户(1 冻结)；5 座电站(沈阳4+大连1，带缩写)；每站 6 台桩(缩写-01~06，
快慢混合、含 1 台故障)；近 3 天已完成订单若干；1 条“充电中”订单(供演示结算)。

## 9. 预留

预约超时自动释放、欠费记录(BR-06)、充值流水/运维日志表、设备心跳与告警、支付/发票/收藏等——按需再加。
