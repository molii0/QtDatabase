# ============================================================
# Git 忽略清单（Qt/qmake + SQLite 项目）
# 被忽略的文件不会进入版本库；修改后无需重新 init。
# ============================================================

# ---- 构建产物（shadow build 目录）----
build/
Makefile*
*.o
*.obj
*.exe

# ---- Qt Creator 个人配置（含本机路径，不要提交）----
.qtcreator/
*.pro.user
.qmake.stash

# ---- 数据库文件（每次运行自动生成 / 变化，不入库）----
*.db
*.db-wal
*.db-shm
*.db-journal

# ---- 运行时目录（预留）----
logs/
avatars/

# ---- 本地整份备份（手工备份用的旧代码，不进 git）----
backup_complex/

# ---- 操作系统垃圾文件 ----
Thumbs.db
Desktop.ini
.DS_Store
