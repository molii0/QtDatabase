## tcpserver.cpp
### namespace{}
C++中的语法接口，匿名命名空间。里面的所有函数、变量、类型，只在当前这个 .cpp 文件内可见。链接器（Linker）不会把它们的符号暴露给其他编译单元（其他的 .cpp 文件）。万一项目其他 .cpp 文件也写了一个叫 statusText 的函数，链接时就会报“重复定义（multiple definition）”错误。包起来后，就算名字重了，也互不影响，非常安全。
### qint64 
Qt特制的64位int类型，由于不同系统上的int位数可能不同，使用这种特制的数据类型可以实现更好的跨平台功能。
### QStringLiteral